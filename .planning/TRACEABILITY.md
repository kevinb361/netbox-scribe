# Traceability: NetBox Scribe — Milestone v0.1

Generated by /saga-verify on 2026-07-21. Source of claims: `.planning/REQUIREMENTS.md`.

Third independent pass — post-close re-verification after the three pre-commit findings were remediated
(S1 bounded non-UTF-8 `validate`, A1 decision-record overclaim, TC-A example verification off the
production path). Every evidence artifact below was opened in this session and every behavioral claim was
re-derived by executing the code, not read from the prior TRACEABILITY.md or AUDIT.md. Baseline this pass:
`make ci` → black clean (15 files), ruff clean, mypy clean (15 source files), `pytest -n auto` →
**33 passed** (was 32).

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| REQ-001 | Installable `nbscribe` CLI exposes help and version without NetBox credentials | **PROVEN** | `tests/test_cli.py::test_help_does_not_require_netbox_credentials`; `::test_version_does_not_require_netbox_credentials` (`tests/test_cli.py:39` asserts a version *pattern*). Re-run credential-free this session: `env -u NETBOX_URL -u NETBOX_TOKEN uv run nbscribe export` → rc 2, `error: NETBOX_URL and NETBOX_TOKEN are required for export`; `--help`/`--version` rc 0 |
| REQ-002 | Full pagination of synthetic NetBox devices; auth/transport/malformed failures reported without leaking the token | **PROVEN** | `tests/test_client.py` — multi-page retrieval, cross-origin `next` rejected before a second credentialed request, repeated-URL loop guard (`client.py:57-59`), 401/403, transport, HTTP 500, malformed JSON and four malformed payload shapes; every failure case asserts the secret is absent and `__cause__ is None`. `client.py:49-111` re-read this pass: no write verbs; `_ensure_safe_next_url` (`client.py:70`) runs before the next request. `tests/test_examples.py` now also drives the real client via `httpx.MockTransport` |
| REQ-003 | `nbscribe export` normalizes devices into schema-versioned canonical YAML, stable ordering, byte-identical for unchanged input | **PROVEN** | `tests/test_exporter.py::test_render_devices_yaml_normalizes_and_sorts_records` pins the exact YAML; `::test_unnamed_devices_receive_stable_exported_identity` (parametrized), `::test_devices_with_missing_name_receive_stable_exported_identity`, `::test_device_without_usable_id_fails_cleanly`; `tests/test_cli.py::test_export_command_writes_canonical_yaml` asserts `first_export == second_export` across two full CLI runs against a live local HTTP server. Re-probed this session: `render_devices_yaml(d) == render_devices_yaml(reversed(d))` for a mixed named/unnamed set; unnamed records resolve to `device-<id>` |
| REQ-004 | Atomic publication: a failed run cannot leave a partial snapshot or replace the last valid one | **PROVEN** | `tests/test_exporter.py::test_atomic_publication_preserves_last_snapshot_and_removes_residue`, `::test_failed_index_publication_does_not_advance_canonical_snapshot`, `::test_failed_canonical_publication_rolls_back_agent_index`, `::test_failed_fetch_does_not_replace_last_snapshot`; `exporter.py:67-109` — same-directory `mkstemp` + `fsync` + `os.replace`, index published first so canonical never advances past its index, index restored or unlinked when canonical publication fails (`exporter.py:99-109`). Re-probed at the CLI: a blocked output parent aborts rc 1 with a one-line `error:` and the index is never created |
| REQ-005 | Every snapshot validates against a published JSON Schema; incompatible versions fail clearly | **PROVEN** | `src/netbox_scribe/schemas/v1/devices.schema.json` (packaged); `exporter.py:42` validates before publish; `tests/test_validation.py::test_validation_rejects_incompatible_schema_version_clearly`, `tests/test_cli.py::test_validate_command_reports_incompatible_schema_version` (rc 1, "unsupported schema version 2; expected 1"), `::test_validate_command_reports_schema_violation`, and new `::test_validate_command_reports_non_utf8_snapshot_without_traceback` (`tests/test_cli.py:200-211`, asserts stderr equals `error: snapshot is not valid UTF-8\n`). Re-run this session: `nbscribe validate` on `\xff\xfe` and on a trailing lone `\x80` → rc 1, bounded message, no traceback; on a directory / missing / unreadable path → rc 1, bounded `OSError` text; `nbscribe validate examples/output/inventory/devices.yaml` → rc 0 |
| REQ-006 | Markdown agent index links canonical records and states source type, freshness, schema version, exporter version | **PROVEN** | `tests/test_cli.py::test_export_command_writes_canonical_yaml:116-129` asserts the index text exactly (derived-view banner, `Source: NetBox`, `Source freshness`, `Schema version: 1`, `Exporter version`, relative link `../inventory/devices.yaml`, device count, name + NetBox ID); `::test_export_command_handles_unnamed_device_without_traceback` asserts the synthesized `` `device-7` — NetBox ID 7``; `tests/test_examples.py::test_synthetic_example_outputs_match_production_export` now byte-compares the committed `examples/output/agent/INDEX.md` against output produced by `export_devices`. Re-derived this session: `exporter._agent_index_record` (`exporter.py:112-117`) projects `id`/`name` from the *normalized* record, so index and canonical YAML cannot disagree on identity |
| REQ-007 | Explicit field/custom-field include/exclude; synthetic tests prove denied values and credentials never appear in output or logs | **PROVEN** | `src/netbox_scribe/policy.py` (custom fields closed by default, deny precedence, unknown-field rejection in `__post_init__` before any NetBox call); `tests/test_exporter.py::test_export_policy_includes_only_allowed_fields_and_deny_wins`, `::test_export_policy_accepts_mandatory_identity_names_but_cannot_exclude_them`; `tests/test_cli.py:112-115` asserts the denied core value, denied custom value, and API token are absent from YAML + Markdown + stdout + stderr combined. Re-probed this session: `--include-field bogus-field` → rc 2 `error: unknown device field(s): bogus-field` with no network call; eight adversarial CLI failure cases re-run and the token `probe-secret-token` appeared in none of the stdout/stderr captures |
| REQ-008 | Public-safe docs and synthetic fixtures cover install, read-only token config, export, validation, Git, agent/RAG consumption | **PROVEN** | `README.md` (install → token → export → policy → validate → Git → agents/RAG → synthetic example → non-goals; `README.md:87` documents the `device-<id>` identity rule); `.env.example` read directly this pass — placeholders only (`https://netbox.example.test`, `NETBOX_TOKEN=replace-me`); `.gitignore:20` re-checked with `git check-ignore -v`; `tests/test_examples.py:42-52` public-safety scan covers `README.md`, `.env.example`, and all of `examples/` with an RFC1918 regex spanning `10/8`, `192.168/16`, and `172.16–172.31`. Mutation-tested this session: a one-character change to either committed example artifact fails `test_synthetic_example_outputs_match_production_export` at the expected assertion, then passes again on restore. README example commands re-executed: `python -m json.tool examples/netbox-devices-page.json` rc 0, `nbscribe validate examples/output/inventory/devices.yaml` rc 0 |

## Gaps (action list)

- **ASSERTED (claimed, unproven):** none — every `[x]` resolved to a concrete artifact that was re-read or re-executed in this session.
- **OPEN (not done):** none.
- **WAIVED:** none. `.planning/decisions/0001-canonical-yaml-derived-markdown.md` and `0002-synthesize-unnamed-device-identity.md` are design records, not gap waivers.

Counts: **PROVEN 8 · ASSERTED 0 · OPEN 0 · WAIVED 0** (8 requirements total).

## Remediation verification (the three pre-commit findings)

| Finding | Claim | Status this pass |
|---------|-------|------------------|
| S1 | Non-UTF-8 `validate` errors are bounded | **CONFIRMED.** `cli.py:90-92` catches `UnicodeError` ahead of `(OSError, SnapshotValidationError)`. Reproduced at the process level on two byte shapes: rc 1, stderr exactly `error: snapshot is not valid UTF-8`, no traceback. The implementer chose `UnicodeError` over the recommended `ValueError`; that is the better fix — `SnapshotValidationError` is the only other reachable `ValueError` and it is already caught, and the narrower catch emits a purposeful message instead of echoing a decode exception's repr |
| A1 | Decision 0002 no longer overclaims collision resistance | **CONFIRMED.** `decisions/0002-...:20` now reads "unique among synthesized identities" and states explicitly that a source device may already carry the same literal name, so consumers must key on the NetBox ID. Behavior re-reproduced: `[{"id": 5, "name": "device-7"}, {"id": 7, "name": null}]` exports two devices both named `device-7`, sorted stably by `(name.casefold(), id)`. The record now matches the behavior. Residual: the caveat has not reached `README.md` — see AUDIT finding A1-r |
| TC-A | Examples run through the production projection | **CONFIRMED.** `tests/test_examples.py:18-39` builds a real `NetBoxClient` over `httpx.MockTransport` and calls `export_devices`, then byte-compares both committed artifacts. Divergence proof re-run this session: through `export_devices` an unnamed device renders `` `device-7` — NetBox ID 7``, while the still-public raw `render_agent_index` renders `` `None` — NetBox ID 7``. The example now pins the shipped path. Both byte assertions mutation-tested live |

## Verifier notes

Traceability is mechanical — claim vs. proof artifact — and it is clean. It is **not** a statement that the
implementation is correct on all legal NetBox or operator input. Independent probing this pass found one
new residual (`validate` still emits a raw `RecursionError` traceback on deeply nested YAML) and one
new documentation gap (the A1 caveat is in the decision record but not in `README.md`); both are recorded
in `.planning/AUDIT.md`. Neither is critical and neither contradicts a requirement's evidence.

One carried accuracy note on `REQUIREMENTS.md` itself, non-blocking: the `make ci with N tests` counts
embedded in the REQ-002/005/006/007/008 evidence lines (13/33/19/20/33) are historical snapshots taken
when each requirement was closed; the suite is now 33. Every named artifact resolves, so the
classifications are unaffected.

`.planning/SPEC.md` holds 8 `### REQ:` blocks against 8 requirements, so the spec library is complete and
`/saga-spec merge` is the next mechanical step — after `.planning/AUDIT.md` is read, since the audit is
the gate, not this table.
